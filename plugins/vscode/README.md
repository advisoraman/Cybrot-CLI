# VS Code / Cursor IDE plugin (tasks)

Copy [`tasks.json`](tasks.json) into your app’s `.vscode/tasks.json` (merge if you already have tasks).

Then: **Terminal → Run Task… → Cybrot Gate: Scan**

Requires `cybrot` on PATH (`npm link` from `cli/`) and **Docker running**.
